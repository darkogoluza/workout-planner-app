import workoutsPlan from "../data/workouts_plan.json";

const workoutPlanReducer = (state = workoutsPlan, actions) => {
  switch (actions.type) {
    default:
      return state;
  }
};

export default workoutPlanReducer;
